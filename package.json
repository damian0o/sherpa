{
  "name": "sherpa",
  "version": "0.2.0",
  "description": "Sherpa — Claude Code marketplace hosting the repo-context plugin (coordination & shared-understanding for one or many repos)",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "gray-matter": "^4.0.3",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
